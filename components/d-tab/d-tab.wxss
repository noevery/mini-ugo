/* components/d-tab/d-tab.wxss */
.d-tab {
    display: flex;
}
.d-tab view {
    flex: 1;
    text-align: center;
    padding: 15rpx 0;
}
.active{
    color:var(--themeColor);
    border-bottom: 5rpx solid currentColor;
}